# ADR Changelog

Terse, chronological log of architecture changes — one line each: `YYYY-MM-DD · summary · (PR #N) · → ADR-00NN`. Full rationale lives in the per-decision
files in this directory; pre-split detail is in [../architecture-history.md](../architecture-history.md).

- 2026-07-13 · Split-branch pipeline results regained `*_ranking` (top12 was a silent 0 in every split-mode history row since 2026-06-11); missing rankings now surface as `null` in summaries; merge-job metrics assembly extracted to pure `_merged_split_metrics` + regression-pinned · (PR #pending) · → ADR-0019
- 2026-06-22 · Retired the FP16-era CUDA-graph GradScaler diagnostics (`FF_AMP_FIXED_SCALE`/`FF_AMP_INIT_SCALE`/`FF_GRADSCALER_TRACE_*` + the scaler-trace machinery in training.py; deleted `src/analysis/cuda_graph_gradscale.py` + test) — vestigial after the FP32 default dropped the default-path GradScaler; numerically inert (env-gated default-off; FP16 opt-in path + skip-detection kept) · (PR #pending) · → ADR-0017
- 2026-06-22 · Batch image cu126→cu130 + torch 2.12.0→2.12.1 (numerics rebaseline, 6-pos retrain); EC2 rollback host g4dn/T4→g6/L4 (T4 retired; disabled path); recommended_cuda_wheel cu130 for sm_75+; deps bumped (scipy/boto3/pytest/ruff; numpy held at 2.4.6 — numba/py3.12) · (PR #pending) · → ADR-0017
- 2026-06-22 · Docs: rescoped the `FF_CUDA_GRAPH` metric-path-divergence / "graphed rebaseline" narrative to the opt-in FP16 path only (the FP32+TF32 default graphed path is per-step bit-exact / effectively inert); added an ADR-0017 Current-status note; fixed stale "FP16 default" prose (AGENTS.md, TODO.md, infrastructure-audit, training.py comments); docs-only · (PR #pending) · → ADR-0017
- 2026-06-22 · Batch GPU Spot capacity fallback fixed: g6-primary/g5-fallback queue order (which only falls back on misconfiguration, never Spot starvation) folded into ONE diversified `ff-gpu-spot` CE (`instanceTypes`=g6.xlarge+g5.xlarge, SPOT_PRICE_CAPACITY_OPTIMIZED); `ff-gpu-spot-g5` retired; infra-only, no retrain · (PR #pending) · → ADR-0013
- 2026-06-22 · Lever A3 optimizer-tail full-iteration capture (FF_CUDA_GRAPH_OPT, autodetect-ON sm_80+) — strictly inert (local Δ=0 gate), ~8-24% of attn-NN step · (PR #pending) · → ADR-0017
- 2026-06-22 · NN training default flipped FP16 autocast → FP32+TF32 (FP16 now opt-in via FF_AMP_DTYPE=fp16); metric-path change, 6-pos rebaseline · (PR #pending) · → ADR-0017
- 2026-06-20 · ADR-0004 condq eager confirmation: the production retrain (`ac3686f`, authoritative) did NOT reproduce the stacked screen wins — RB/TE flat, WR +0.046 RMSE regress; Ridge byte-identical (attention-only); KEPT enabled for RB/WR/TE as an owner forward-bet for #1210, not a measured win; serving healthy post-roll (all attn_nn, no NaN; `ecs_rollout` red only on a `services-stable` waiter timeout) · → ADR-0004
- 2026-06-20 · ADR-0004 `condq` (context-conditioned attention queries, #121) ENABLED for RB + WR + TE: an RMSE re-screen (`condq-screen-stacked-80494dd`) found it MAE-flat but tail-trimming (RB attn-NN RMSE −0.051±0.028, 24/24; TE tail gain at a small median cost); RB/TE earned on the screen, **WR an owner forward-bet** (WR regresses on the screen — attributed to a CB-level matchup-feature data gap, not a broken mechanism; re-screen when those features land, #1210); QB/K/DST stay OFF (QB overfit; K/DST noise); plumbed via `PositionConfig.attn_condition_queries_on_static` through both the cfg + `registry` served-kwargs paths (arch-changing retrain) · (PR #1198) · → ADR-0004
- 2026-06-19 · ADR-0004 default-OFF attention-architecture extension ablation (MAE round): no flag (#109–121) beats the AttentionPool baseline on MAE; `selfattn` regresses (RB Δ +0.116 / QB +0.076, eager `launch_ablate` #1180); none adopted on MAE — `condq` later adopted for RB on the RMSE re-screen (above) · → ADR-0004
- 2026-06-19 · ADR-0020 Batch path extended to the eager `ablation_runner` family: new launcher `src/tuning/launch_ablate.py` + container entry `src/tuning/ablate_batch.py` ride the `--mode=tune` dispatch via `FF_TUNE_ABLATE_MOD`, reusing the `ff-ab-job` clone + per-cell S3 checkpoint/resume; gives the vmap-incompatible `selfattn` arm a GPU home (eager FP16 A/B, `--cuda-graph false` default) · (PR #1180) · → ADR-0020
- 2026-06-15 · ADR-0021 #1172 stacked-path fixes: `ab_knob_doe` runs the FULL pipeline (disabling the non-attn models dropped the stacked Phase-A into an early return that omits `test_df` → KeyError on every cell); `ab_feature_screen` sources family→columns from `PositionConfig.include_features`/`_FAMILY_COLS` and declares `expect_ridge_identical=False` (reading the absent runtime `cfg["include_features"]` silently no-opped the drop → null ΔRidge=0); unit fixtures now assert against the real built CONFIG; Status stays Proposed, stacked re-validation deferred (no GPU spend) · (PR #1176) · → ADR-0021
- 2026-06-15 · Opponent-defense attention branch DISABLED for QB/RB/WR/TE (`opp_attn_history_stats=[]`): 24-seed stacked Batch A/B (`src/tuning/ab_opp_def.py`) showed it does not improve the attn NN (QB −0.074 / RB −0.027 / TE −0.013 MAE, WR +0.009 within noise), overturning #214's single-seed claim; attention-only (static `opp_def_*_L5` kept for Ridge/LGBM); DST opp-offense untouched · (PR pending) · → ADR-0004
- 2026-06-15 · `FF_CUDA_GRAPH_FULL` promoted opt-in → production default (autodetect-ON sm_80+, force-off override): prod training captures gather+fwd+loss+bwd in one graph; owner-approved second graphed rebaseline (per-step bitwise-exact, FP16+GradScaler trajectory drift ~0.5% worst-target like model-only); Batch-validated (determinism + bounded divergence) before promotion; `cuda_graph_full_active` stamped into History rows; next 6-position retrain rebaselines · → ADR-0017
- 2026-06-15 · Proposed: knob DoE (#720 re-homed) + net-new feature-family selection screen as stacked `ab_harness` specs — reuse the validated vmap stacked path + Ridge sentinel + #720 PB/main-effects engine instead of hand-rolling vmap or a fresh DoE engine; CPU logic unit-tested, stacked GPU run pending off-box validation · (PR pending) · → ADR-0021
- 2026-06-11 · Batch GPU execution path for the shared A/B harness: per-position Spot jobs ride the `--mode=tune` env dispatch (`FF_TUNE_AB_SPEC` → `src/tuning/ab_batch.py`), cells checkpoint per-cell JSONs to `ab_runs/{run_id}/` for Spot resume, launcher (`src/tuning/launch_ab.py`) clones an `ff-ab-job` definition for arbitrary image SHAs, batch-image.yml gains non-main guards (no `:latest`, no job-def registration), ab-batch.yml dispatch workflow added · → ADR-0020
- 2026-06-11 · Split Batch training enabled as production default (`BATCH_SPLIT_ACTIVE=true`) after first full 6-position split run: 18 jobs green, merge deps honored, Δ=0.0000 vs same-image monolithic baseline across all models/positions · → ADR-0019
- 2026-06-11 · Spot G+VT quota raised 24→64 vCPU (OD G+VT 4→24); live GPU CEs + setup.sh `MAX_VCPUS` raised to 64 and the GPU-CE reconcile branch now diffs/updates `maxvCpus` — two concurrent fan-outs (or train + tune fleet) no longer starve at the CE ceiling · → ADR-0013
- 2026-06-11 · train-batch wait/append hardened: 5h wait (`FF_WAIT_TIMEOUT=18000`, job ceiling 350m), post-timeout recovery step re-checks submitted Batch job ids (launch.py `FF_BATCH_JOB_IDS_FILE` breadcrumb) and proceeds with the benchmark append + dedup marker when all SUCCEEDED, else files a tracking issue + fails loud; b8d3f3f/2fd5449 benchmark rows backfilled (Spot starvation had silently skipped them) · → ADR-0013
- 2026-06-11 · tune_nn forces eager (FF_CUDA_GRAPH=0 → scheduler_v2 namespace) for thread-backend runs with n_jobs>1 on would-capture hosts: torch.cuda.graph global-mode capture races other trial threads' in-flight GPU work (cudaErrorIllegalState, Batch g6/L4 job 614ae7d7); mps backend and n_jobs==1 stay graphed · → ADR-0017
- 2026-06-10 · Split Batch training into opt-in GPU NN branch + c8a CPU Ridge/LightGBM branch + merge-only publisher; branch artifacts stage under `split-runs/{run_id}/{POS}/{branch}/`, CPU jobs use dynamic-core sequential leases, and GPU/CPU job-definition revisions are pinned independently · → ADR-0019
- 2026-06-08 · Batch Spot queue now prefers g6/L4 but falls back to g5/A10G for capacity: setup.sh creates/reconciles separate CEs (`ff-gpu-spot` order=1, `ff-gpu-spot-g5` order=2) instead of one mixed instanceTypes CE, because Batch CE order preserves g6 preference while Spot allocation inside one CE can choose any pool; A10G maps to g5 in benchmark labels and Batch NN tuning auto-MPS now covers A10G · → ADR-0013/0017
- 2026-06-08 · Upcoming-week live inputs (round 2): wired `practice_status` (nflverse load_injuries primary, exact training Full/Limited/DNP→2/1/0 encoding; free Sleeper players-dump fallback when nflverse empty in-season, month-gated for offseason) + current-season `contract_*` (nflverse load_contracts/OTC via the training derive_active_contracts; advances age/years vs the carry-forward). New src/serving/live_sources.py (defensive); `_fill_current_week_context` precedence live → carry-forward → default. Holistic source map (researched): ESPN=real-time slate, nflverse=official slow feeds (practice/contracts), Sleeper=practice fallback only; weather (Open-Meteo) deferred, no paid feeds. Same-frame A/B: contracts land (Lamar attn 19.46→19.32), practice offseason no-op, board sane. Serving-only, no retrain · (PR #1101) · → ADR-0018
- 2026-06-08 · Upcoming-week 2026 inputs: wired the player's LIVE ESPN depth chart → `depth_chart_rank` (precedence live → carry-forward → default) + live injury `game_status`, replacing a carry-forward that reached 2 seasons back (a no-latest-season backup inherited a starter rank → the attn NN ranked Ravens QB2 Skylar Thompson above Lamar Jackson, attn 17.6 vs 19.5). `espn_live.fetch_depth_chart_ranks` (core /depthcharts, re-rank + clamp to nflverse [1,3], prior-season fallback) + `fetch_injury_status_map` (→ loader.py status encoding); same-frame A/B Skylar attn 17.6→4.3, board sane. practice_status (no ESPN data) / weather / contracts deferred. Serving-only, no retrain · (PR #1092) · → ADR-0018
- 2026-06-08 · Serving predictions cache/snapshot row contract now includes scoring-aware NFL.com and RotoWire expert projections, with a cache schema version to force hydrate misses for old caches; Season Leaders gets persisted column visibility and modal expert lines · → ADR-0014
- 2026-06-08 · Exposed per-position TabPFN-3 tuning levers — `tabpfn_softmax_temperature` (calibration), `tabpfn_auto_scale_n_estimators` (honor `n_estimators` exactly), `tabpfn_inference_config` (dict passthrough to the advanced `InferenceConfig`, e.g. `REGRESSION_Y_PREPROCESS_TRANSFORMS`); all default to TabPFN's own values, so inert until changed. Threads `PositionConfig → build_pipeline_config → _build_tabpfn → _new_regressor`. Still default-off, benchmark-only · → ADR-0003
- 2026-06-08 · Upgraded the `TabPFNMultiTarget` comparison variant v2 → **TabPFN-3** (latest; `tabpfn` 8.x default, `model_path='auto'`): faster (KV-cached, ~20× over 2.5) + stronger (TabArena pareto-dominant), runs every position in-regime (no `ignore_pretraining_limits`); only wrapper change is the 8.x `n_jobs`→`n_preprocessing_jobs` rename. WR pilot (single seed): TabPFN-3 is the **best model at 3.912 total FP MAE** (beats the Attn NN 3.989; v2 was 2nd at 4.004), ~90s fit (v2 ~355s). Now **non-commercial-licensed** → benchmark-only, can never be served (v2 was the last servable checkpoint); still default-off, dormant, not in requirements. Decision unchanged — independent comparison, no ensemble · → ADR-0003
- 2026-06-08 · Added `TabPFNMultiTarget` (TabPFN v2 pretrained tabular transformer) as an opt-in, **default-off** 5th model variant — dormant infrastructure: never trained, served, or displayed in prod, and `tabpfn` is not in any requirements file (lazy import, CI unaffected); WR pilot was competitive (2nd of 5; best of all models on receptions) but did not beat the Attention NN and is transductive / ~27× slower (ran 2.5× over TabPFN's ~10k-sample limit); decision unchanged — independent comparison, no ensemble · → ADR-0003
- 2026-06-08 · Upcoming-week artifact build moved OUT of serving → scheduled CI job: two in-serving deploys rolled back (worker OOM + a runtime 2025-PBP download failure, even at 4 vCPU/8GB) because a 2-worker serving task can't host load_raw_data+build_features+inference; new [refresh-upcoming-week.yml](../../.github/workflows/refresh-upcoming-week.yml) (cron 3h + on train-batch completion + manual) builds + uploads upcoming_week.json to S3, serving only downloads + serves it (`sync_artifact_from_s3`), reverted to 2 vCPU/4GB; deploy.yml drops the 20-min stability wait · (PR #1076) · → ADR-0018
- 2026-06-08 · Live upcoming-week predictions homepage (new default serving tab): QB/RB/WR/TE projections for the next unplayed week; live slate (schedule/odds/rosters/injuries) from ESPN's free API ([src/serving/espn_live.py](../../src/serving/espn_live.py)); synthetic rows spliced onto nflverse history + run through `build_features` for byte-identical featurization; in-serving 3h background poller serves an off-disk artifact (`/api/upcoming_week`); QB cold-start inflation found+fixed (carry `depth_chart_rank`/contracts forward + health defaults); serving-only, no retrain · (PR #1069) · → ADR-0018
- 2026-06-07 · Cold-start image-slim (Stage 1): Batch training image base swapped conda `pytorch/pytorch:2.12.0-cuda12.6-cudnn9-runtime` (~3.7GB compressed pull) → `nvidia/cuda:12.6.3-base-ubuntu24.04` (~86MB) + pip `torch==2.12.0+cu126` (same version; `*-base` flavor so the wheel brings its own CUDA libs, no `*-cudnn-runtime` double-count); attacks the ~122s pull half of the ~258s cold-start; torch-distribution change ⇒ NN rebaselines within noise, Ridge byte-identical; build-time import smoke; first built on merge (no PR build / no local daemon); 6-position retrain · → ADR-0013
- 2026-06-07 · `benchmark.py` becomes the autodetect front door for local multi-position training: `main()` resolves `-j` (autodetect → all positions on a CUDA box with ≥12 cores, else sequential) and lazily delegates to `parallel_train.orchestrate`, so the documented `python -m src.benchmarking.benchmark` now fans out in parallel on the 9950X3D/5080 instead of looping sequentially; `--sequential`/`-j 1` force the in-process loop; reuses the existing core-pool engine, orchestration-only (numerically inert), no retrain · → ADR-0017
- 2026-06-07 · "Warm serving cache" `/warm` poll split out of `train-batch.yml`'s train job into its own parallel `warm_cache` job (`needs: train`) so its up-to-180s poll no longer delays the benchmark-history commit + dedup success-marker write (~3min sooner); best-effort + ALB-only, unchanged `/warm` semantics; CI-ordering only, no retrain · → ADR-0014
- 2026-06-07 · SOCI cold-start optimization removed — never worked on Batch: ECS-EC2 `amazon-ecs-agent` doesn't pull through the soci snapshotter (Fargate-only, containers-roadmap#1832); a fully-correct config still pulled in 103s, and this is the ECS-agent's limit not the GPU arch (so the g4dn→g6 migration doesn't unblock it). Removed `ff-batch-lt` LT + `userdata.sh` + the CI index-publish step; cold-start is the ~120s full pull it always was · → ADR-0013
- 2026-06-07 · Benchmark History hardware label is now **derived at runtime** by `src/batch/benchmark.py` from `gpu_name` + `cuda_graph_active` (and `sm`) that `src/batch/train.py` stamps into each position's `benchmark_metrics.json`, superseding `train-batch.yml`'s hardcoded `g6.xlarge (Spot, CUDA-graph)` string (now only a fallback) so the label tracks the live CE — and the `, CUDA-graph` suffix — with no workflow edit if the hardware ever changes again; the `train.py` path-trigger fires a numerically-inert 6-position retrain (metadata-only — Ridge-identity tell holds) · → ADR-0017
- 2026-06-07 · Live `ff-gpu-spot` Batch CE actually migrated `g4dn.xlarge`→`g6.xlarge` (the 2026-05-31 flip silently no-op'd — setup.sh's reconcile path only updated the launch template, never `instanceTypes`); reconcile extended to diff+update `instanceTypes`; re-applies the g6/L4 docs reverted in #936 now that the migration is genuinely deployed (graphs autodetect ON → graphed rebaseline) · → ADR-0013
- 2026-06-05 · `FF_CUDA_GRAPH` reversed to **autodetect-ON for sm_80+** (g6/L4 + 5080; env demoted to a force-off override): deliberately moves the sm_80+ metric path per-arch (non-byte-identical to CPU/CI) for the launch-bound ~1.5-1.8×, graphed benchmark rebaseline, no pre-merge A/B; supersedes the same-day opt-in plumbing entry below; fires a 6-position retrain · → ADR-0017
- 2026-06-05 · Plumbed the production-Batch opt-in for `FF_CUDA_GRAPH` (new `FF_BATCH_CUDA_GRAPH` repo variable → `train-batch.yml` → `launch.py` forwards it into each position's `containerOverrides`); unset by default so production stays eager/byte-identical pending an L4 quality A/B; graphed benchmark rows self-label `g6.xlarge (Spot, CUDA-graph)` for a clean rebaseline; no retrain · → ADR-0017
- 2026-06-05 · D4: per-game game-script + final-score tokens added to WR/QB/TE/K attention history (RB already carried them) — closes the environment-blind history gap so the NN can learn matchup-driven week-to-week variance; config-only (columns already train-time-merged by build_position_features, no splits regen); retrains WR/QB/TE/K · → ADR-0004
- 2026-06-01 · D1: benchmark-history CLIs' `--cv` is now a deprecated alias for rolling-origin walk-forward reporting; internal expanding-window CV remains for ad-hoc/tuning callers; no retrain · → ADR-0001
- 2026-06-01 · Batch NN tuner: six-position g6 fan-out retained, `--parallel-backend auto` resolves native-Linux L4/g6 to 3 NVIDIA MPS workers with `FF_CUDA_GRAPH=1` while Mac/5080 stay on thread mode; study namespace split to `scheduler_v2_mps_graph`; tuner-only, production defaults unchanged · → ADR-0017
- 2026-06-01 · D17: LightGBM Optuna tuner self-starts core-pool leasing for parallel local trials and defaults to seed-averaged trial scoring over 42,43,44 with seed-versioned study storage; tuner-only, no retrain · → ADR-0017
- 2026-06-01 · D15: issue #720 attention-knob experiment runner adds test-MAE fANOVA and Plackett-Burman DoE screens over eight attention-only knobs; tuner-only, no retrain · → ADR-0015
- 2026-05-31 · D4: newest-first attention history ordering (index 0 = most-recent game) across all 3 sequence builders, so the learned positional embedding is recency-indexed; ALiBi formula kept correct (games_ago == position); 6-position retrain · → ADR-0004
- 2026-06-01 · Corrected stale ADR references after config consolidation, unused display constants, and EC2 rollback sequencing docs; documentation-only · → ADR-0002/0004/0006/0007/0009
- 2026-06-01 · Corrected D13 g6 Spot per-run cost estimate to match operator runbook math; documentation-only · → ADR-0013
- 2026-05-31 · Split ARCHITECTURE.md into per-decision ADR files under docs/adr/; froze the prior Update history to architecture-history.md · → index in [../ARCHITECTURE.md](../ARCHITECTURE.md)
- 2026-05-31 · D12: launch-bound NN hot-path cuts (−15.6% QB) + `torch.compile` measured +169%/+62% (stays off on all archs); fused-AdamW MAE cost isolated deterministically to +0.89% base-NN / −0.18% attention · (PR #655) · → ADR-0012
- 2026-05-31 · D1: per-position `min_games_per_season` knob — RB/WR/TE→1, QB deferred; triggers a 6-position retrain · (PR #656) · → ADR-0001
- 2026-05-31 · Distributed 48 decision-scoped entries from the frozen Update history into their per-decision `## Changelog` sections; 13 non-decision entries (serving/analysis/CI/tooling) remain in architecture-history.md · → all ADRs
- 2026-05-31 · Local parallel trainer: work-conserving CPU core pool (`src/shared/core_pool.py`) replaces the static per-slice `LGBM_N_JOBS` (immutable post-spawn) — each position leases `ceil(cores/active)` per CPU stage so cores freed by finished positions widen survivors' thread counts; numerically inert, opt-in via `FF_CORE_POOL_ADDR`; dispatch order from measured `benchmark_history` · (PR #670) · → ADR-0017
- 2026-05-31 · `FF_CUDA_GRAPH` opt-in CUDA-graph NN training (sm_80+, off by default): 1.84× attn-NN speedup but NOT bit-inert (~0.5% eval drift — FP16+GradScaler amplifies a per-step-exact graph; LN/FP32/det-stop fixes all fail/cost the speedup) → local-iteration knob, not for benchmark A/Bs; off-by-default ⇒ production byte-identical · → ADR-0017
- 2026-06-01 · CUDA-graph benchmarkability follow-up: graph-vs-graph clean; BN warmup restore inert; fixed-scale 512 removes GradScaler schedule drift but graph-vs-eager still differs at fixed 2130 steps, so `FF_CUDA_GRAPH=1` A/Bs need graphed rebaselines and remain incomparable to eager baselines · → ADR-0017
- 2026-06-10 · tune_nn study namespace keys off the actual capture decision (`cuda_graph_enabled()`), not raw `FF_CUDA_GRAPH` truthiness (force-OFF override post-cutover); completes PR #1120 (which added the thread-graph namespace `scheduler_v2_graph` the env-keying never selected) · → ADR-0015
- 2026-06-11 · `FF_CUDA_GRAPH_FULL` opt-in full-step CUDA-graph capture (gather+forward+branchless-loss in one graph; tune jobs default-on in isolated `*_graphfull` study namespaces, training default-off pending Batch gates) · → ADR-0017
- 2026-06-15 · Prediction cache moved to an off-container CI build (refresh-splits builds + uploads it before the ECS roll, via `src/scripts/build_serving_cache.py`) + serving task bumped to 8 GB (deploy.yml now pins cpu/memory from `infra/aws/task-definition.json`) after a splits-refresh fingerprint drift left fresh tasks OOM-looping the in-container recompute · → ADR-0014
- 2026-06-15 · Ported the two stackable legacy NN ablations to `ab_harness` specs (`ab_attn_arch` drops `entropy`; `ab_scheduler_type` drops `plateau`) so they inherit the GPU-default N=24 stacked-seed regime; `rb_gate`/`batch_lr` evaluated and deliberately left eager · (PR pending) · → ADR-0020
- 2026-06-19 · Post-train ECS rollover re-added to `train-batch.yml` (`ecs_rollout` job: `force-new-deployment` + `wait services-stable`), partially reverting the 2026-05-21 simplification: the in-flight poller can't heal an architecture-changing retrain (#1175's opp-def-branch removal left serving load_state_dict'ing a two-branch checkpoint against single-branch code → NaN attn for QB/RB/WR/TE for ~4 days), so a per-train forced rollover guarantees a clean boot-time reload while the poller stays the between-train fast-path · (PR pending) · → ADR-0014
- 2026-06-22 · Warm pre-pulled GPU AMI for Batch cold-start: `infra/batch/build-warm-ami.sh` bakes the training image's base layers into an ECS-GPU AMI; `FF_BATCH_AMI_ID=<ami> setup.sh` attaches it to the GPU CE via a version-pinned `ff-warm-ami-lt` launch template (opt-in, default-unset no-op, GPU-CE-scoped) to skip the ~122 s image pull on the orchestration-bound cold-start · (PR pending) · → ADR-0022
- 2026-07-02 · Upcoming-week inference frame widened to season-to-date (mid-season attention-history/L3/`season_starts_to_date` cold-start fix, #1411; predictions sliced back to week W) + odds-less slate `spread_line` None guard (#1400) · (PR pending) · → ADR-0018
- 2026-07-04 · Docs: ADR-0020 context/changelog rescoped to the FP32+TF32 (AMP-off) fleet default (FP16 opt-in via FF_AMP_DTYPE=fp16, graphs effectively inert on the default); stale FP16 / 2.0-per-δ prose fixed in AGENTS.md, SETUP.md, ADR-0015 · (PR pending) · → ADR-0020
- 2026-07-05 · Serving dashboard rebranded to the Fantasy Football Predictor design system: midnight blue-black surface ramp + electric-blue `--accent` (green demoted to `--positive` success semantics), position/model hues promoted to CSS custom properties, new logo-mark favicon, and a persisted OLED night mode (`html[data-theme="oled"]`, header sun/moon toggle, pre-paint inline script; Chart.js colors now read the active theme's tokens) · (PR pending) · → ADR-0023
- 2026-07-05 · Frontend rewritten as React (sources `src/serving/frontend/`, esbuild-committed bundle at the old `static/js/app.js` path, Node-free runtime, CI `frontend-bundle` staleness guard); design-system component primitives adopted under `frontend/src/ds/` · (PR pending) · → ADR-0023
- 2026-07-05 · Changelog & Timeline dashboard tab: `/api/timeline` (live weekly per-source MAE incl. experts, per-week winner, common-rows edge, season summary) + committed owner-curated `src/serving/release_changelog.json` (seeded from real benchmark_history milestones) + React TimelineView (8th tab, `#timeline`; existing History keeps `#history`) · (PR pending) · → ADR-0023
- 2026-07-05 · Upcoming-week artifact gains NFL.com + RotoWire projections: CI builder fetches both feeds for the slate week (`load_nflcom_with_gsis_id` gains a `weeks=` passthrough with its own cache key; Sleeper already weekly), projects via the existing core helpers, folds an expert digest into the rebuild signature; homepage renders the columns feature-detected (old artifacts show the classic 3 columns) · (PR pending) · → ADR-0018
- 2026-07-06 · NextWeek "rank" sort flipped to per-position best-ranker head selection (LightGBM for RB/WR; selection, not ensembling) + FFToday backfill vet CLEAN (2013/2019/2024 sample) with the loader's season-list cache-key collision fixed (`_seasons_sig`, cache v2) + tracked expert-edge action plan (todo/expert-edge-action-plan.md) · (PR pending) · → ADR-0003
