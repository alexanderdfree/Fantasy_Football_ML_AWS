@AGENTS.md

# CLAUDE.md — Claude Code specifics

Everything above (imported from [AGENTS.md](AGENTS.md)) is the shared, provider-neutral project brain — read it first. **This file adds only the Claude-Code-specific machinery**: hooks, skills, the scheduled audit routine, sub-agent/Workflow contracts, and auto-memory. The Codex-side parity stub for the same disciplines is in [AGENTS.md](AGENTS.md) § "Codex specifics" — keep the two in sync when you change a shared discipline.

## Hooks (`.claude/settings.json` → `.claude/hooks/`)
- **`session-start.sh`** (SessionStart) — best-effort pull of Claude memory from S3 (always); then, **only in remote/web sessions** (`CLAUDE_CODE_REMOTE=true`), bootstrap the venv, install deps (requirements + torch CPU + dev), and write `VIRTUAL_ENV`/`PATH`/`PYTHONPATH` to `$CLAUDE_ENV_FILE`. Local sessions stop after the memory pull (use SETUP.md for the venv).
- **`ruff-format.sh`** (PostToolUse: `Edit|Write|MultiEdit`) — `ruff format` the edited `.py` (silent, fail-open).
- **`guard-worktree-path.sh`** (PreToolUse: `Edit|Write|MultiEdit|NotebookEdit`) — blocks (exit 2) any write whose `file_path` is under the parent root but **not** under `$CLAUDE_PROJECT_DIR`, echoing the corrected worktree path. This is the deterministic backstop for the "edit the worktree, not the parent" gotcha (see [AGENTS.md](AGENTS.md) § Worktree workflow): plan files / sub-agents report parent-absolute paths, and using them verbatim silently edits `main`'s checkout — `git status` stays clean and a benchmark re-run shows MAE Δ=0.0000 (the late smell). Burned ~30 min GPU time on PR #284; recurred on #354, #370, #378. **The manual re-prefix-then-grep habit is still the goal; the hook just catches the miss.** Recovery if it slips: `cp <parent>/<file> <worktree>/<file>` (if committed baselines match) or `git -C <parent> diff -- <file> | git -C <worktree> apply`, then `git -C <parent> checkout -- <file>`.
- **`pre-pr.sh`** (PreToolUse: Bash `gh pr create`) — the deterministic gate: **B1** ruff check + ruff format --check + `pytest -m unit`; **B2** benchmark-freshness for edits to position pipeline files (`src/{pos}/config.py|features.py|targets.py|run_pipeline.py`), requiring a fresh `benchmark_history/*.json` or `{pos}/outputs/models/` mtime *after* the edit. Additive-only edits bypass B2; `[docs-only]` bypasses both. It recognizes fresh per-position outputs, so don't force a 10-min full sweep — fix the gate / surface friction instead.
- **`post-pr-create.sh`** (PostToolUse: Bash after `gh pr create`) — injects the autonomous follow-up workflow below (it only injects the instruction; Claude executes it).
- **`memory-sync-stop.sh`** (Stop) — best-effort `scripts/agent-memory-sync.sh all push`, pushing changed local Claude/Codex memory trees to their separate S3 prefixes.

## The pre-PR / post-PR flow
- **Before `gh pr create`, invoke the `pre-pr-judge` skill.** It spawns a worker subagent to diff the branch against `origin/main`, compare against the original task, and flag scope creep — the "agent did more than I asked" failure mode behind the reverted shared-venv ([#110](https://github.com/alexanderdfree/Fantasy_Football_ML_AWS/pull/110)/[#111](https://github.com/alexanderdfree/Fantasy_Football_ML_AWS/pull/111)) and gunicorn `--preload` ([#148](https://github.com/alexanderdfree/Fantasy_Football_ML_AWS/pull/148)/[#149](https://github.com/alexanderdfree/Fantasy_Football_ML_AWS/pull/149)) PRs. Skip only for trivial changes — see the skill for the skip list. **Always `git fetch origin main && git rebase origin/main` first**, or the judge sees unrelated newer-on-main files as scope creep.
- **After `gh pr create`, the `post-pr-create.sh` hook injects an autonomous follow-up.** Claude's next turn: (1) `git fetch origin main --quiet && git rebase origin/main` to fold in newly merged PRs (force-push if the rebase changed anything; abort + surface on conflict), (2) invoke the `/review` skill on the PR, (3) auto-apply **every** finding via Edit/Write **except** architectural / design-level ones — nit/minor/style, naming, formatting, dead imports, local logic fixes, localized security fixes, missing-test additions, missing edge-case handlers, doc/comment tweaks, (4) commit auto-applied fixes as `review: address /review nits` and push, (5) if any architectural / design-level finding was surfaced, stop and list them for the user, (6) otherwise wait for green CI via `gh pr checks <N> --watch` then `gh pr merge <N> --squash` and `git push origin --delete <branch>` (worktree-safe). CI failure → stop and surface; never `--admin` bypass. **Exception — solve-issues PRs** (branch `audit-*/tier-*`): step (6) becomes a stop for **explicit user merge sign-off** (show the diff + Tier C benchmark deltas and ask) — the `solve-issues` skill mirrors this.

## Sub-agent contract — two shapes, picked by batch size
The provider-neutral *consolidation pattern* (file-disjoint bundles → tier-by-risk PRs) is in [AGENTS.md](AGENTS.md); this is the Claude `Agent`/`Workflow` orchestration detail.
- **>10 items** (default for code-review remediation): spawn **as many file-disjoint worker sub-agents as possible**, but workers **DRAFT** commits in their isolated worktrees and do **NOT** push or open PRs. The orchestrator cherry-picks each risk-tier's worker commits onto a fresh staging branch from `origin/main` and opens **one PR per tier**. See auto-memory `feedback_tier_by_risk_pr_consolidation` for the per-tier recipe.
- **<10 truly independent tasks:** the older per-worker-opens-PR shape is fine — each worker commits, pushes, and runs `gh pr create` itself.
- **Either shape:** workers MUST report ship-status (commit SHA, branch, files modified, findings skipped + why); the orchestrator verifies via `gh pr list` / `git log <worker-branch>` — incomplete worker output is a recurring failure mode the orchestrator catches.
- **Claude orchestration gotchas:** worker `pytest` MUST be foreground (a backgrounded run ends the turn before pytest finishes and the transcript is reclaimed); `isolation: worktree` + `run_in_background: true` returns no `worktreePath:` — confirm via `git worktree list | grep agent-`; if a worker runs out of usage with a correct partial diff, take over its worktree directly rather than respawning; checklist finding→worker before spawning and reconcile fixed+leave==total before declaring done; in a `Workflow` script, inline large arrays as `const FILES = [...]` rather than passing them via `args` (`args.map` throws if the harness coerces args to a non-array).

## Scheduled routines
A Claude Code cloud routine (a claude.ai scheduled remote agent) runs a **codebase audit** every 2h: it fans out parallel auditor subagents, dedupes against open+closed `claude-audit` GitHub issues, and files **one issue per finding** — labeled `claude-audit` + severity (`severity-high`/`severity-medium`) + area — plus one closed `[claude-audit] checkpoint` issue per fire recording the audited SHA. Those per-finding issues are what the [`solve-issues`](.claude/skills/solve-issues/SKILL.md) skill triages, orders by severity, and bundles into tier-by-risk PRs. The actionable backlog is `gh issue list --label claude-audit --label severity-high` for the priority (high-severity) slice; `solve-issues` triages both `severity-high` and `severity-medium` (checkpoints carry no severity label).

The routine's prompt is version-controlled here, not only in the dashboard. **[.claude/routines/audit/](.claude/routines/audit/) is the source of truth:**
- [`prompt.md`](.claude/routines/audit/prompt.md) — the full audit prompt (what actually runs).
- [`shim.md`](.claude/routines/audit/shim.md) — the thin pointer deployed to the dashboard; at run time it reads `prompt.md` from the `main` checkout and executes it verbatim.
- [`config.json`](.claude/routines/audit/config.json) — deploy params (trigger id, model, cron, environment, allowed tools).
- [`README.md`](.claude/routines/audit/README.md) — the edit/push recipe and gotchas.

**To change what the audit does:** edit `prompt.md`, merge to `main` — the next fire picks it up, no dashboard push needed. **To change a deploy param or the shim itself:** edit `config.json` / `shim.md`, then invoke `/schedule` and say "push the audit routine" (see the README). `prompt.md` must stay at exactly that path on `main`, or the shim stops without auditing rather than improvising.

## After a non-routine session, invoke the `post-session-critique` skill
Run when the user corrected your approach mid-flight, a non-obvious convention bit you, or something went unusually well because of a specific rule. It captures *prompt* lessons (proposed AGENTS.md, CLAUDE.md, or auto-memory edits) the way [todo/fixed-archive.md](todo/fixed-archive.md) captures *code* lessons. Skip if the session was routine.

## Auto-memory
Claude loads this project's auto-memory automatically each session. The auto-memory dir is **machine-local** by design (official Claude Code behavior) but is synced across the owner's machines via a private S3 bucket by [scripts/agent-memory-sync.sh](scripts/agent-memory-sync.sh) / [scripts/claude-memory-sync.sh](scripts/claude-memory-sync.sh) (#686; see [SETUP.md](SETUP.md) § "Sync agent auto-memory across machines"). Codex memory is synced separately under `codex-memory/<repo>/memories/`; Claude keeps `claude-memory/<repo>/memory/`. That sync is a cross-machine **cache** of incidental recall, **not** a source of truth. [AGENTS.md](AGENTS.md) § "Operating lessons" is the **cross-agent mirror** of the durable subset; the convention going forward: when `post-session-critique` writes a **durable, cross-agent** lesson (ML method, git/PR/CI/env workflow, project fact), add it to AGENTS.md and to any relevant agent memory stores. **Claude-internal harness lessons stay in Claude auto-memory only** — e.g. held/replayed burst output → send ONE call and wait (never fan out 50 identical commands); re-verify garbled/truncated tool output with one clean sequential call before reporting a number; the predictions-bootstrap `#loading-overlay` eats `preview_click`, so drive serving tabs via `preview_eval`; seed the `data/serving_cache/` triple (parquet+metrics+fingerprint) to browser-verify the serving UI with no model artifacts.
