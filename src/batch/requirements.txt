# torch is no longer provided by the base image — the slim nvidia/cuda:*-base
# base (cold-start image-slim change) ships no torch, so install it here. Pin the
# SAME version the old pytorch/pytorch:2.12.0-cuda12.6-cudnn9-runtime base shipped
# (2.12.0 / cu126) so training numerics stay comparable; this is a packaging
# change, not an upgrade. The L4 (sm_89) uses the cu126 build (mirrors
# requirements-gpu.txt's cu130 pin for the Blackwell dev box). uv resolves this
# +its nvidia-*-cu12 deps from the extra index; UV_INDEX_STRATEGY=unsafe-best-match
# (set in Dockerfile.train) lets PyPI serve the rest.
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.12.0+cu126
numpy==2.4.6
pandas==3.0.3
pyarrow==24.0.0
nflreadpy==0.1.5
polars==1.41.2
scikit-learn==1.9.0
scipy==1.17.1
matplotlib==3.11.0
joblib==1.5.3
lightgbm==4.6.0
mord==0.7
boto3==1.43.31
optuna==4.9.0
