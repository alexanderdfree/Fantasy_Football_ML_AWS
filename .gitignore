# Python bytecode
__pycache__/
*.pyc
*.pyo

# Data (large parquet files)
data/raw/
data/splits/

# Serving prediction cache (see src/serving/app.py::_persist_cache_to_disk)
data/serving_cache/

# Demo media (host externally, never commit — see AGENTS.md)
*.mov
*.mp4

# Model artifacts (all positions)
**/outputs/models/
**/outputs/figures/
*.pkl
*.pt

# Hyperparameter tuning databases (Optuna) + per-run results JSON
*.db
tune_*_results.json

# Feature-engineering disk cache (see src/shared/feature_cache.py)
.cache/

# Per-run test_df cache for src/analysis/attn_weekly_accuracy.py
.attn_cache/

# Analysis output
analysis_output/

# Benchmark history in-flight writes (see shared/benchmark_utils.append_to_history)
benchmark_history/**/*.tmp

# Per-position logs from the parallel local trainer (scripts/train-local-parallel.sh)
/logs/

# Jupyter
.ipynb_checkpoints/

# Environment
.env
.venv/
venv/

# OS
.DS_Store

# pytest
.pytest_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/

# Ruff
.ruff_cache/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Python packaging
*.egg-info/
dist/
build/

# AWS bootstrap resource-ID outputs
infra/aws/.env.out

# Claude Code
.claude/*
!.claude/hooks/
!.claude/settings.json
!.claude/skills/
!.claude/routines/

# Credentials (preventive)
*.pem
*.key
*.p12
*.pfx
*.jks
*.crt
*.cer
credentials*
secrets*
.env.*
!.env.example
