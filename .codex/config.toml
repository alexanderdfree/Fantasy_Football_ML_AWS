# Repo-level Codex defaults. Keep hook trust hashes, auth state, and
# machine-specific model catalog paths in user configuration, not this file.
service_tier = "fast"
# Model, reasoning and context/compaction defaults come from the user/runtime.
# Do not pin a historical workstation profile in this shared project config.

[features]
fast_mode = true
