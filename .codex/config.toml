# Repo-level Codex defaults. Keep hook trust hashes, auth state, and
# machine-specific model catalog paths in user configuration, not this file.
service_tier = "fast"
# Leave model, reasoning, context, and compaction settings to personal defaults
# or an explicit session selection.

[features]
fast_mode = true
