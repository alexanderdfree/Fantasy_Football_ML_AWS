# Review of the archived WR investigation, 2026-09-23

The archived fitting jobs ran on **2026-09-18 UTC**, finishing between 03:02:28
and 03:03:40 UTC according to `execution.json`. Their 2025 comparisons are
retrospective. The publication index seals the original README, findings and
numerical results; those files remain byte-for-byte unchanged during this review.
Rebasing the diagnostic code does not relabel the historical worker or data.

The current code adds two safeguards for any future invocation: preparation
requires AWS Batch because it fits a `StandardScaler`, and the explicit
experiment configuration verifies the archived numerical-source hashes before
fitting. It also requires the experiment's recorded eager FP32 regime. These
checks do not rerun or replace the completed evidence. Pure recovery, inspection,
reporting, and the no-fit unit tests can run locally.
Preparation also rechecks every recovered file against its receipt and rejects
unrecorded inputs before creating interventions; new manifests retain both the
verified receipt hash and historical numerical-source hashes.

The original README's description of preparation as “not model fitting” must
not be read as permission for local execution: scaler fitting also belongs on
Batch. Follow the pinned historical worker instructions for reproduction; the
current production checkout is not the historical numerical recipe.

This investigation does not qualify a model-default change. The active repair
round still requires earlier-season development, frozen candidates, paired
2024/2025 confirmation, improvement in both MAE and RMSE, and non-increasing
protected-cohort errors. Missing genuine archive coverage blocks promotion.
The older recommendation for a future calibration study is not part of this
round: no new calibration model, fantasy-total training, or test-derived offset
is authorized by this evidence. PRs #1575/#1568 retain their separate gates.
