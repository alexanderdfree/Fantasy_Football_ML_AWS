> Historical record. Validate current code, configuration and ADRs before applying the recorded fix.

### [FIXED] QB rookie early-game calibration added as a phase-only static feature
- **File(s):** [src/qb/features.py](../../src/qb/features.py) (split-aware rookie phase labels), [src/qb/config.py](../../src/qb/config.py) (whitelist `rookie_early` for QB models and attention static), [tests/qb/test_features.py](../../tests/qb/test_features.py) and [tests/qb/test_feature_contract.py](../../tests/qb/test_feature_contract.py) (contract coverage), [src/analysis/rookie_cohort_findings.md](../../src/analysis/rookie_cohort_findings.md) (seed-42/43 subgroup evidence).
- **What:** The rookie cohort metric showed QB rookies were systematically over-predicted in their first three games, but overall MAE hid it because the subgroup is small and low-scoring. The stale implementation plan also assumed Ridge was the current best QB model; after the latest mainline run the served model is LightGBM, so the ship decision had to be made on LightGBM's subgroup metrics.
- **Fix:** Compute split-aware `is_rookie` internally, expose only the phase-specific `rookie_early` feature to QB models, and feed it to the attention static branch as non-temporal context. A flat `is_rookie` feature was tested and rejected: it barely moved LightGBM rookie bias and hurt global/backtest metrics. The phase-only cut improved production-best LightGBM rookie-early bias in both validation seeds (`+0.882→+0.619`, `+0.849→+0.626`) and rookie-early MAE in both seeds (`3.625→3.526`, `3.611→3.573`), with global/backtest MAE effectively flat and top-12 mixed but near-flat.
- **Lesson:** Keep the rookie fix phase-aware. A pooled rookie flag averages together early over-prediction and later under-prediction, so it mostly adds noise; judge this class of change on the served model's subgroup bias/MAE and ranking, not on overall MAE alone.
