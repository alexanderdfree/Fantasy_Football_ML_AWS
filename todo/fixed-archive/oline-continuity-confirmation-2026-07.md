### [CONFIRMED — ship pending] E2 O-line continuity for TE (expert-edge Phase 2b)

Historical experiment from 2026-07-06, consolidated on 2026-09-11. The recorded
source, data and evaluation regime bound these results; no production feature
is activated and no current accuracy claim is made here.
- **File(s):** [../src/tuning/ab_oline_continuity.py](../../src/tuning/ab_oline_continuity.py) (screen), [../src/tuning/ab_oline_confirm.py](../../src/tuning/ab_oline_confirm.py) (rolling-origin RotoWire confirm), tests alongside, `data/raw/rotowire_slate_ppr_2018_2025.parquet` (on S3). Runs `ab_oline_continuity-…105238Z` (screen) + `ab_oline_confirm-…112527Z` (confirm), commit `df7e796`.
- **What:** the B2 runner-up — starting-five stability from PFR snap_counts (the repo keeps only skill-player `offense_pct`, discards OL rows). Three season-to-date team-week features (distinct top-5 starters / prev-game overlap / stable streak), W-1-lagged, whitelisted into every model + the attention static branch.
- **Result — CONFIRMED for TE on its served (attention) ranker.** Screen (2025): the attention head's deep-lineup regret improved (TE regret@12 −2.25, 3/3 seeds; QB −2.82, 2/3), MAE flat; served RB/WR (LightGBM) flat. Confirm (rolling-origin 2022–2025 on the RotoWire-covered slate): E2 lowers the **attention** head's lineup regret in **TE 3/4 origins** (Δ −3.9/−4.7/−0.5; +1.4 in 2022), pushing the TE attention head *below* RotoWire's regret in 2/4 origins. **RB 3/4** (−3.9/−6.4/−3.9) but RB is LightGBM-served and E2 doesn't beat RotoWire there (stranded on a non-served head). **QB 1/4 → not confirmed** (screen signal was noise). This is the first Phase-2 lever to pass its confirm gate.
- **Lesson:** contrast B2 (real-but-flat, gain on a non-served Ridge slice) — E2's gain lands on a **served** head (attention, which serves TE/QB per the Phase-1a ranking) and **replicates across rolling-origin seasons on the expert-covered slate**, which is what promotes a screen signal from "noise-band" to "shippable." Two method notes that mattered: judge the confirm on the **served head per position** (TE=attention, RB=LightGBM — the RB attention gain is real but unshippable as-is), and the confirm's rolling-origin + RotoWire-slate is what separated the durable TE signal from the noise QB one (single-season screen had both at ~2-3/3). The RotoWire crosswalk was GitHub-403 in the remote env; the **rosters carry both `sleeper_id` and `player_id`(gsis)**, so the slate was built from that bridge — reusable trick when nflverse ID feeds are blocked. **Ship for TE is a separate owner-gated retrain PR** (wire into TE `INCLUDE_FEATURES` + `ATTN_STATIC_FEATURES` + the loader; benchmark evidence); nothing wired yet.
