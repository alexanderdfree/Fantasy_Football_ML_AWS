### [FIXED] DST scoring semantics, historical expert eligibility and offline comparison cohorts

- **File(s):** `src/shared/comparison_scoring.py`, `expert_eligibility.py`, `evaluation_cohorts.py`, `src/serving/{core,comparison}.py`, `src/analysis/{comparison_frames,topn_expert_gap,tier_expert_comparison,analysis_expert_comparison}.py` (PR pending).
- **What:** The paired web comparison still included differently defined DST points allowed. ESPN's actual differed from the model label in 53/544 team-weeks; ESPN and Sleeper also disagreed on PHI Week 4 (25/23) and ARI Week 15 (34/40). Offline comparators accepted backfilled NFL.com offense before 2024, and top-N/tier reports retained full-score truth or different source populations. A counterexample reported 33.33 model MAE on three rows beside 0 expert MAE on two identical shared rows.
- **Fix:** Exclude points allowed from every DST comparison sum while preserving normal fantasy targets/totals. Store dedicated full-precision-derived comparison totals in cache schema 10, require reference recipe v3, and expose the component exclusion. Centralize the NFL.com cutoff at projection and metric boundaries. Use raw shared-component actuals, native K/DST frames, preselected cohorts and a common finite forecast intersection in offline reports; retire source-specific projected-rank error slices.
- **Lesson:** Equal column names and scoring weights do not establish equal statistical quantities. Validate provider semantics, select comparison cohorts independently of forecast coverage, and enforce source eligibility after cache reads too. Retaining a raw archive for diagnosis must not make it eligible for accuracy claims.
