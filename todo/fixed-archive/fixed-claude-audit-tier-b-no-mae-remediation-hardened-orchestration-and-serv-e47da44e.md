> Historical record. Validate current code, configuration and ADRs before applying the recorded fix.

### [FIXED] Claude-audit Tier B no-MAE remediation hardened orchestration and serving guards
- **File(s):** Batch/EC2 workflow fixes in [../.github/workflows/](../../.github/workflows), operator helpers in [../src/batch/](../../src/batch), serving display/error hardening in [../src/serving/app.py](../../src/serving/app.py), K/DST CLI parity in [../src/k/run_pipeline.py](../../src/k/run_pipeline.py) and [../src/dst/run_pipeline.py](../../src/dst/run_pipeline.py), test-scope routing in [../src/scripts/scope_positions.py](../../src/scripts/scope_positions.py), and matching unit coverage under [../tests/](../../tests).
- **What:** The Tier B audit batch found drift in non-model-output surfaces: workflow SHA/revision pinning, artifact manifest priority, CPU queue selection, stale health/error reporting, serving wiki formulas, manual CLI device parity, and test-scope false positives.
- **Fix:** Kept the pass no-MAE by avoiding target/config/model-math changes, then aligned Batch/EC2 freshness checks and SHA stamping with env prefixes, made artifact readers prefer smoke-tested `stable` manifests, added CPU queue routing, hardened serving errors to store `repr(e)`, corrected formula strings, added K/DST `--device`, and covered benchmark-history/test-shard routing.
- **Lesson:** Low-risk audit remediation still needs path-aware scoping. Manifest consumers, workflow concurrency, and test detection are operational contracts; fix them with targeted tests and defer target-file or benchmark-comparability cleanups to a run that can provide pipeline evidence.
