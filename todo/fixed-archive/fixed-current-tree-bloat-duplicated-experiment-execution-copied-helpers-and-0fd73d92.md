> Historical record. Validate current code, configuration and ADRs before applying the recorded fix.

### [FIXED] Current-tree bloat: duplicated experiment execution, copied helpers, and unused skip markers
- **File(s):** `src/tuning/_execution.py`, `src/tuning/ab_harness.py`, `src/tuning/ablation_runner.py`, `src/tuning/ablate_batch.py`, shared offline helpers in `src/tuning/` and `src/analysis/`, `src/analysis/sleeper_loader.py`, `.github/workflows/skip-sentinel.yml` (removed), `benchmark_history/` (391 placeholder records removed), `AGENTS.md`, `agent-workflows/operating-lessons.md`; implementation `99e977ed`.
- **What:** Eager ablations and A/B specs duplicated process orchestration and output isolation; Sleeper, feature-audit, topology, and subgroup helpers had copied implementations. The skip workflow kept creating records that both the web and iOS History views hide. Provider mechanics and detailed operating lessons made the startup orientation unnecessarily long. A generated `tune_rb_gate_results.json` was still tracked despite its ignore rule.
- **Fix:** Share a fresh-process executor and output context while retaining the eager job/report API, primed-cache behavior, timing-clean serial mode, and all stacked defaults. Reuse serving-safe Sleeper code with the research reader preserved; extract the repeated offline helpers and reuse Batch checkpoint/provenance helpers. Retire skip-marker generation and remove only explicitly flagged placeholders, preserving all actual run records and legacy API parsing. Move the complete operating reference into a linked document and replace repeated provider mechanics with pointers. Untrack the generated gate result while retaining the local ignored file and a temporary backup.
- **Lesson:** File/line counts locate maintenance surface, not deletable code. The Batch launchers already shared job-definition and manifest plumbing; their different CLI/env/report contracts remain useful. Verify actual clients before retiring bookkeeping, preserve serving-image import boundaries, and prove shared execution with real serial/parallel Ridge predictions plus failure/isolation tests. The retained 12-variant gate snapshot had baseline MAE 4.2270535 and its smallest MAE 4.2216799 at `gate_hidden=24 (w=3.0)`; it has no reliable run provenance and is not new model evidence.
